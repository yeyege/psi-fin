- 文档采用中文 Markdown，API 规范以表格形式逐接口列出方法、URL、请求体与状态码，便于人工阅读与对照实现。
- 演示站点使用内联 `<style>` 与语义化 class（如 `.card.system/.bi/.roadmap`）区分不同预览入口，保持零依赖、单文件可独立打开。
- 所有静态页面声明 `lang="zh-CN"` 与 UTF-8 编码，并通过相对路径引用同级资源（favicon、SVG logo），确保在 GitHub Pages 根目录下正确加载。